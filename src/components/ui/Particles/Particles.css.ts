import { style } from "@vanilla-extract/css";

export const styles = {
    root: style({
        justifyContent: "center",
        alignItems: "center",
        width: "100vw",
        height: "100vh",
        minHeight: "100vh",
        zIndex: 0,
    }),
};
